---
title: "Importation et manipulation d'images"
subtitle: "Chapitre 1 — Traitement d'images satellites avec Python"
author: "Foucher, Apparicio, Bouroubi, Germain, Clabaut"
jupyter: python3
---

## Objectifs {.smaller}

::: {.incremental}
- Connaître les bibliothèques Python pour **lire** une image
- Accéder aux **métadonnées** avant de la lire
- Comprendre les **formats** (RVB vs géo-formats)
- Manipuler la **matrice de pixels** avec `numpy`
- Exploiter la **géoréférence** avec `rioxarray` / `xarray`
:::

## Quatre caractéristiques d'une image {.smaller}

::: {.incremental}
1. **Matrice de pixels** — le cube de données (NumPy)
2. **Dynamique** — le type de stockage (`uint8`, `int16`, `float32`…)
3. **Nombre de bandes** — de 3 (RVB) à des centaines (hyperspectral)
4. **Métadonnées** — dimensions, date, et surtout la **géoréférence**
:::

## NumPy, le fondement {.smaller}

![Opérations de base sur les matrices NumPy](../images/41586_2020_2649_Fig1_HTML.png){width="72%" fig-align="center"}

::: {.notes}
Insister : toute image est d'abord un tableau NumPy. Indexation `[ligne, colonne, bande]`, découpage, et la distinction vue vs copie.
:::

## Démo — lecture et vraies couleurs {.smaller}

L'ordre des bandes de `RGBNIR_of_S2A.tif` est **B, V, R, PIR** → on réordonne en R, V, B :

```{python}
#| fig-align: center
import rioxarray as rxr
import matplotlib.pyplot as plt

img = rxr.open_rasterio('../RGBNIR_of_S2A.tif')     # data au racine du dépôt
fig, ax = plt.subplots(figsize=(5, 3.5))
img.sel(band=[3, 2, 1]).plot.imshow(vmin=86, vmax=5000, ax=ax)  # R, V, B
ax.set_title("Composé vraies couleurs"); ax.axis('off')
plt.show()
```

## Regarder les métadonnées avant de lire {.smaller}

```{python}
#| code-line-numbers: "2-6"
import rasterio
with rasterio.open('../RGBNIR_of_S2A.tif') as src:
    print("Bandes     :", src.count, "| type :", src.dtypes[0])
    print("Dimensions :", src.height, "x", src.width)
    print("CRS        :", src.crs)
    print("Résolution :", src.res, "m")
```

## Indexation avancée — bande dominante {.smaller}

`argmax` sur l'axe des bandes → la bande la plus brillante en chaque pixel :

```{python}
#| fig-align: center
import numpy as np
cube = rxr.open_rasterio('../RGBNIR_of_S2A.tif').to_numpy()   # (4, H, W) ; B,V,R,PIR
dominante = cube.argmax(axis=0)                               # 0..3
fig, ax = plt.subplots(figsize=(5, 3.6))
im = ax.imshow(dominante, cmap='viridis'); ax.axis('off')
ax.set_title("Bande dominante (0=B, 1=V, 2=R, 3=PIR)")
fig.colorbar(im, ax=ax, ticks=[0, 1, 2, 3], shrink=0.7); plt.show()
```

Le PIR domine sur la **végétation**. Masquer (`np.ma`) et sauvegarder (`np.save`/`.npz`) complètent la boîte à outils.

## Découpage — `clip_box` {.smaller}

Extraire une emprise plus petite (boîte englobante, dans le CRS de l'image) :

```{python}
#| fig-align: center
import numpy as np
img = rxr.open_rasterio('../RGBNIR_of_S2A.tif')
minx, miny, maxx, maxy = img.rio.bounds()
cx, cy = (minx + maxx) / 2, (miny + maxy) / 2
dx, dy = (maxx - minx) / 4, (maxy - miny) / 4
sous = img.rio.clip_box(cx - dx, cy - dy, cx + dx, cy + dy)
print("forme :", tuple(img.sizes.values()), "->", tuple(sous.sizes.values()))
fig, ax = plt.subplots(figsize=(4.5, 3))
sous.sel(band=[3, 2, 1]).plot.imshow(vmin=86, vmax=5000, ax=ax)
ax.set_title("Quart central"); ax.axis('off'); plt.show()
```

## Rééchantillonnage — `reproject` {.smaller}

Changer la résolution spatiale ; méthode d'agrégation au choix (moyenne pour réduire) :

```{python}
from rasterio.enums import Resampling
bas = img.rio.reproject(img.rio.crs, resolution=20, resampling=Resampling.average)
print("résolution :", img.rio.resolution(), "->", bas.rio.resolution())
print("forme      :", tuple(img.sizes.values()), "->", tuple(bas.sizes.values()))
```

## Sélection par étiquette — `sel` / `isel` {.smaller}

`xarray` sélectionne par **valeur** (`sel`) ou par **position** (`isel`) ; on peut nommer les bandes :

```{python}
print("sel(band=4) == isel(band=3) :", bool((img.sel(band=4) == img.isel(band=3)).all()))
img_n = img.assign_coords(band=['B', 'V', 'R', 'PIR'])
print("bande PIR par étiquette :", img_n.sel(band='PIR').shape)
```

Un résultat se réécrit en GeoTIFF géoréférencé avec `rasterio` (`profil` source + `dst.write`).

## Points clés {.smaller}

::: {.incremental}
- **GDAL** = lecture/écriture de l'imagerie (drivers de formats)
- **RVB** (au plus 3 bandes, 8 bits) vs **géo-formats** (multi-bandes, géoréférence)
- Consulter les **métadonnées avant** de lire (`rio info`, `gdalinfo`, `rasterio`)
- Une image est un **tableau NumPy** — découpage, vue vs copie
- **Indexation avancée** (`argmax`, tableaux d'indices), **tableaux masqués** (`np.ma`), **sauvegarde** (`.npy`/`.npz`)
- `rioxarray` / `xarray` : dimensions nommées + géoréférence via `.rio`
- **Découper** (`clip_box`), **rééchantillonner** (`reproject`), sélectionner par **étiquette** (`sel`/`isel`), **écrire** un GeoTIFF (`rasterio`)
:::

## À vous de jouer {.smaller}

- Ouvrez `RGBNIR_of_S2A.tif`, affichez ses bandes, type, CRS et résolution
- Extrayez une fenêtre de 100 × 100 pixels au centre et affichez-la
- Reprojetez en `EPSG:4326` puis sauvegardez en GeoTIFF (`rio.to_raster`)
- Découpez au quart central (`clip_box`), rééchantillonnez à 20 m, et écrivez un NDVI en GeoTIFF (`rasterio`)

##  {background-color="#357cc0"}

::: {style="text-align:center; color:white;"}
### Des questions ?
Chapitre suivant : **Réhaussement et visualisation**
:::
